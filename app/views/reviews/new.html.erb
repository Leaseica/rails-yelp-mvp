 <!-- app/views/restaurants/:restaurant_id/reviews/new.html.erb -->
<h1>Add a review to </h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>

<%= link_to 'All restaurants', restaurants_path %>
